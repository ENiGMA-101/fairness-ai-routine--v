# Durable PostgreSQL storage for production (Neon, Supabase pooler, etc.).
# Local example: postgresql://postgres:postgres@127.0.0.1:5432/app_db
DATABASE_URL=

# Private /setup console, CSV export, and source ZIP download.
# Set a unique random value of at least 24 characters; without it, access is disabled.
# Example generation: openssl rand -hex 32
ADMIN_TOKEN=
